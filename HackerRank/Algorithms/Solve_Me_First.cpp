#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vll = vector<ll>;
ll solveMeFirst(ll &x, ll &y)
{
    return x + y;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll a, b;
    cin>>a>>b;
    cout << solveMeFirst(a, b);
}