#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using vll = vector <ll>;
int main ()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    ll n, i, j, x = 0, t;
    cin>>n;
    if ((n % 2) == 0)
    {
        cout<<"Mahmoud";
    }
    else
    {
        cout<<"Ehab";
    }
}